schema_version: 1
name: fault_no_meta_replication_selftest
description: >-
  Self-test smoke profile for missing metadata replication. Keeps generic
  self-test bounded while preserving the torn-metadata failure mode.

platform: platforms/cortex_m0_nvm.repl
flash_backend: nvm_ctrl
bootloader:
  elf: examples/vulnerable_ota/firmware.elf
  entry: 0x10000000

memory:
  sram: { start: 0x20000000, end: 0x20020000 }
  write_granularity: 8
  slots:
    exec:    { base: 0x10000000, size: 0x38000 }
    staging: { base: 0x10038000, size: 0x38000 }

images:
  staging: examples/vulnerable_ota/firmware.bin

success_criteria:
  vtor_in_slot: exec

fault_sweep:
  mode: runtime
  max_writes: 512
  max_writes_cap: 4096
  run_duration: "0.01"

expect:
  should_find_issues: true

schema_version: 1
name: mcuboot_pr2199_move_fixed
description: >
  MCUboot swap-using-move AFTER PR #2199 fix (commit 33094fc5).
  Stuck revert: primary has valid image + REVERT trailer state, secondary is
  erased. Fixed MCUboot detects the stuck state and erases BOTH secondary
  AND primary trailer — clears REVERT state, boots primary cleanly.

platform: platforms/cortex_m4_flash_fast.repl
flash_backend: faultFlash
bootloader:
  elf: results/oss_validation/assets/oss_mcuboot_pr2199_move_fixed.elf
  entry: 0x00000000
memory:
  sram: { start: 0x20000000, end: 0x20040000 }
  write_granularity: 4
  slots:
    exec: { base: 0x0000C000, size: 0x76000 }
    staging: { base: 0x00082000, size: 0x76000 }
# Stuck revert scenario — same setup as broken profile.
images:
  exec: results/oss_validation/assets/zephyr_slot0_padded.bin
pre_boot_state:
  # Primary trailer magic (4 words of BOOT_IMG_MAGIC at 0x81FF0)
  - { address: 0x00081FF0, u32: 0xF395C277 }
  - { address: 0x00081FF4, u32: 0x7FEFD260 }
  - { address: 0x00081FF8, u32: 0x0F505235 }
  - { address: 0x00081FFC, u32: 0x8079B62C }
  # Primary copy_done = 0x01 (SET)
  - { address: 0x00081FE0, u32: 0x00000001 }
  # Primary image_ok stays 0xFF (erased/UNSET) — triggers REVERT row
success_criteria:
  vtor_in_slot: exec
  # After fix: primary trailer magic should be cleared (erased = 0xFFFFFFFF).
  marker_address: 0x00081FF0
  marker_value: 0xFFFFFFFF
fault_sweep:
  mode: runtime
  evaluation_mode: execute
  max_writes: auto
  max_writes_cap: 200000
  run_duration: "2.0"
  max_step_limit: 20000000
  fault_types: [power_loss, interrupted_erase]
  hash_bypass_symbols: ["bootutil_img_validate"]
expect:
  # Fixed MCUboot clears the stuck state — 0 failures expected.
  should_find_issues: false

schema_version: 1
name: mcuboot_head_move_nrf52_revert_nohashbypass
description: >
  No hash bypass — full SHA-256 validation in emulation.
  Zero-day hunt: MCUboot HEAD swap-move revert on nrf52840dk.
  Default geometry (448KB slots). Images swapped, upgrade unconfirmed —
  MCUboot should revert to original. Different-sized images.

platform: platforms/cortex_m4_flash_fast.repl
flash_backend: faultFlash
bootloader:
  elf: results/oss_validation/assets/oss_mcuboot_head_move_nrf52.elf
  entry: 0x00000000
memory:
  sram: { start: 0x20000000, end: 0x20040000 }
  write_granularity: 4
  slots:
    exec: { base: 0x0000C000, size: 0x76000 }
    staging: { base: 0x00082000, size: 0x76000 }
# Revert: images already swapped, upgrade not confirmed.
# Primary has staging image (new), secondary has exec image (old).
images:
  exec: results/oss_validation/assets/zephyr_head_move_nrf52_slot1.bin
  staging: results/oss_validation/assets/zephyr_head_move_nrf52_slot0.bin
# Primary trailer: magic=GOOD, copy_done=SET, image_ok=UNSET → REVERT.
pre_boot_state:
  - { address: 0x00081FF0, u32: 0xF395C277 }
  - { address: 0x00081FF4, u32: 0x7FEFD260 }
  - { address: 0x00081FF8, u32: 0x0F505235 }
  - { address: 0x00081FFC, u32: 0x8079B62C }
  - { address: 0x00081FE0, u32: 0x00000001 }
success_criteria:
  vtor_in_slot: exec
  marker_address: 0x0000C014
  marker_value: 0x00000001
fault_sweep:
  mode: runtime
  evaluation_mode: execute
  max_writes: auto
  max_writes_cap: 200000
  run_duration: "20.0"
  max_step_limit: 200000000
expect:
  should_find_issues: false

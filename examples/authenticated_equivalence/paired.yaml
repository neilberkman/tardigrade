schema_version: 2
name: synthetic_paired_authenticated_equivalence
input_hex: a00441555448a104010203041003617070200444415441110102
grammar:
  kind: tlv
  type_width: 1
  length_width: 1
  byteorder: little
  max_records: 32
  max_record_length: 64
authentication:
  boundary_type: 160
  signature_type: 161
  include_boundary: true
mutations:
  max_mutants: 1
  cases:
    - id: paired-copy-and-patch
      operations:
        - operation: duplicate
          source: {type: 32, occurrence: 1}
          patches:
            - {offset: 0, bytes_hex: "58"}
        - operation: patch
          target: {type: 17, occurrence: 1}
          patches:
            - {offset: 0, bytes_hex: "03"}
harness:
  command: [python3, parser.py, --input, "{input}", --mode, vulnerable]
  timeout_seconds: 5
mode: vulnerable
outcomes:
  - accepted
  - committed
  - version
  - target
  - size
  - installed_payload_digest
  - rollback_outcome
  - security_state
